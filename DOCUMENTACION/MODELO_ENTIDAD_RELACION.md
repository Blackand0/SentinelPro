# MODELO ENTIDAD RELACIÓN - SENTINEL PRO

```
┌─────────────────┐
│   COMPANIES     │
├─────────────────┤
│ id (PK)         │
│ name            │
│ email (UNIQUE)  │
│ admin_id (FK)   │
└────────┬────────┘
         │ 1:N
         ├─────────────────┐
         │                 │
    ┌────▼──────────┐  ┌───▼──────────┐
    │    USERS      │  │ DEPARTMENTS  │
    ├───────────────┤  ├──────────────┤
    │ id (PK)       │  │ id (PK)      │
    │ username (U)  │  │ name         │
    │ email (U)     │  │ manager_id   │
    │ password      │  │ budget       │
    │ role          │  │ company_id   │
    │ company_id    │  └──────────────┘
    │ department_id │
    └────┬──────────┘
         │ 1:N
    ┌────▼──────────┐
    │  PRINT_JOBS   │
    ├───────────────┤
    │ id (PK)       │
    │ user_id (FK)  │
    │ printer_id    │
    │ document_name │
    │ page_count    │
    │ paper_type_id │
    │ printed_at    │
    └───────────────┘


┌──────────────────┐     ┌──────────────┐     ┌───────────────┐
│    PRINTERS      │     │ PAPER_TYPES  │     │TONER_INVENTORY│
├──────────────────┤     ├──────────────┤     ├───────────────┤
│ id (PK)          │     │ id (PK)      │     │ id (PK)       │
│ name             │     │ name         │     │ name          │
│ location         │     │ size         │     │ brand         │
│ model            │     │ price_sheet  │     │ price_unit    │
│ ip_address       │     │ stock        │     │ stock         │
│ company_id (FK)  │     │ company_id   │     │ min_stock     │
│ department_id    │     └──────────────┘     │ printer_id    │
└─────┬────────────┘                         │ company_id    │
      │ 1:N                                  └───────────────┘
      │
┌─────▼──────────────────┐
│ MAINTENANCE_LOGS       │
├────────────────────────┤
│ id (PK)                │
│ printer_id (FK)        │
│ technician_id          │
│ maintenance_type       │
│ cost                   │
│ status                 │
│ scheduled_date         │
│ completed_date         │
│ company_id (FK)        │
└────────────────────────┘


┌───────────────────────┐     ┌──────────────┐
│CONSUMPTION_EXPENSES   │     │   ALERTS     │
├───────────────────────┤     ├──────────────┤
│ id (PK)               │     │ id (PK)      │
│ company_id (FK)       │     │ company_id   │
│ expense_type          │     │ type         │
│ amount                │     │ title        │
│ description           │     │ severity     │
│ date                  │     │ read         │
└───────────────────────┘     │ resource_id  │
                              └──────────────┘

┌──────────────┐
│   SESSION    │
├──────────────┤
│ sid (PK)     │
│ sess (JSON)  │
│ expire       │
└──────────────┘
```

**Total Entidades:** 11  
**Relaciones:** 1:N (companies → users, departments, printers, paper_types, toner_inventory, maintenance_logs, consumption_expenses, alerts)  
**Cardinalidad:** Uno a muchos con foreign keys
